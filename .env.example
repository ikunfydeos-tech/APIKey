# API Key Manager 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ===== 必需配置 =====

# 数据库连接 (PostgreSQL)
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/api_manager

# JWT 密钥 (至少 32 字符，建议使用随机字符串)
SECRET_KEY=your-secret-key-at-least-32-characters-long

# API Key 加密密钥 (32 字节，可用 openssl rand -base64 32 生成)
ENCRYPTION_KEY=your-32-byte-encryption-key-here

# 加密盐值 (16 字节)
ENCRYPTION_SALT=your-16-byte-salt

# CORS 允许的域名 (逗号分隔)
CORS_ORIGINS=http://localhost:8000,https://yourdomain.com

# ===== 可选配置 =====

# 运行环境: development / production
ENV=production

# 每分钟请求限制
RATE_LIMIT_PER_MINUTE=60

# 远程模型配置 URL (可选)
MODEL_CONFIG_URL=
